name: blank

on:
  push:

jobs:
  royal:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        lag: ["missile", "chip", "picnic", "spill", "label"]
        waterproof: ["control", "tide", "launch", "perfect"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar jj
